import 'package:flutter/material.dart';

import 'app/app.dart';

class BlocPlingoApp extends StatelessWidget {
  const BlocPlingoApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PlingoApp();
  }
}



import 'package:flutter/material.dart';

class GraphQlHome extends StatelessWidget {
  const GraphQlHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Lul graph ql'),
        ),
      ),
    );
  }
}